diff --git a/linux-sandbox/src/landlock.rs b/linux-sandbox/src/landlock.rs
--- a/linux-sandbox/src/landlock.rs
+++ b/linux-sandbox/src/landlock.rs
@@ -249,6 +249,12 @@ fn apply_seccomp_filter(
                 )?,
             ];
-            if managed_network.is_some_and(|context| context.dangerously_allow_all_unix_sockets) {
+            // Seccomp cannot inspect the pathname later passed to connect(2),
+            // so AF_UNIX socket creation must be allowed whenever the effective
+            // managed-network policy contains at least one Unix-socket grant.
+            if managed_network.is_some_and(|context| {
+                context.dangerously_allow_all_unix_sockets
+                    || !context.allow_unix_sockets.is_empty()
+            }) {
                 denied_socket_conditions.push(SeccompCondition::new(
                     0,
                     SeccompCmpArgLen::Dword,
diff --git a/linux-sandbox/tests/suite/managed_proxy_unix_sockets_tests.rs b/linux-sandbox/tests/suite/managed_proxy_unix_sockets_tests.rs
--- a/linux-sandbox/tests/suite/managed_proxy_unix_sockets_tests.rs
+++ b/linux-sandbox/tests/suite/managed_proxy_unix_sockets_tests.rs
@@ -24,7 +24,7 @@ impl ConfigReloader for TestConfigReloader {
 }
 
 #[tokio::test]
-async fn managed_proxy_allow_all_unix_sockets_preserves_network_isolation() {
+async fn managed_proxy_unix_socket_grants_preserve_network_isolation() {
     if let Some(reason) = managed_proxy_skip_reason().await {
         eprintln!("skipping managed proxy Unix socket test: {reason}");
         return;
@@ -64,6 +64,7 @@ async fn managed_proxy_allow_all_unix_sockets_preserves_network_isolation() {
         .expect("nonblocking listener");
     let server = std::thread::spawn(move || {
         let deadline = Instant::now() + OPERATION_TIMEOUT * 2;
+        let mut remaining_requests = 2;
         loop {
             match listener.accept() {
                 Ok((mut stream, _)) => {
@@ -75,7 +76,10 @@ async fn managed_proxy_allow_all_unix_sockets_preserves_network_isolation() {
                     stream.read_exact(&mut request).expect("read Unix request");
                     assert_eq!(request, *b"ping");
                     stream.write_all(b"pong").expect("write Unix response");
-                    break;
+                    remaining_requests -= 1;
+                    if remaining_requests == 0 {
+                        break;
+                    }
                 }
                 Err(error) if error.kind() == std::io::ErrorKind::WouldBlock => {
                     assert!(Instant::now() < deadline, "Unix request timed out");
@@ -127,7 +131,7 @@ for family in (socket.AF_NETLINK, getattr(socket, 'AF_VSOCK', 40)):
     let cwd = std::env::current_dir().expect("current directory");
     for (label, dangerously_allow_all_unix_sockets, allow_path) in [
         ("default denial", false, false),
-        ("path grant stays restricted", false, true),
+        ("path grant permits Unix sockets", false, true),
         ("explicit allow-all", true, false),
     ] {
         let mut env = create_env_from_core_vars();
@@ -165,6 +169,6 @@ for family in (socket.AF_NETLINK, getattr(socket, 'AF_VSOCK', 40)):
             .arg(&socket_path)
             .arg(tcp_addr.port().to_string())
-            .arg(if dangerously_allow_all_unix_sockets {
+            .arg(if dangerously_allow_all_unix_sockets || allow_path {
                 "allow"
             } else {
                 "deny"
